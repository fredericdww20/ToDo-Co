{% extends 'base.html.twig' %}

{% block body %}
    <div class="row">
        {{ form_start(form, {'action' : path('task_create')}) }}
            {{ form_widget(form) }}

            <button type="submit" class="mt-3 btn btn-success pull-right">Ajouter</button>
        {{ form_end(form) }}

    </div>
    <a href="{{ path('task_list') }}" class="mt-3 btn btn-success pull-right">Retour à la liste des tâches</a>
{% endblock %}
